{
  "name": "yapayzeka-bot",
  "description": "Telegram bot with AI responses",
  "repository": "https://github.com/Rq7mg/Yapayzeks",
  "stack": "heroku-24",
  "env": {
    "TOKEN": {
      "description": "Telegram bot token",
      "required": true
    },
    "OPENAI_API_KEY": {
      "description": "OpenAI API key",
      "required": true
    },
    "TARGET_USER_ID": {
      "description": "Telegram ID to respond to",
      "required": true
    }
  }
}
